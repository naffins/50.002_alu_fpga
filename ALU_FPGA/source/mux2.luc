module mux2 (

    // Simple 1-selector mux;
    
    input inp[2], // The two inputs, for s=0 and s=1 respectively
    input s, // Selector bit
    output out // Result
  ) {

  always {
    out = (inp[0] & (~s)) | (inp[1] & s);
  }
}
